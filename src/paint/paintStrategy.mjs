
class PaintStrategy {
    constructor (config) {
        this.config = config;
    }

    paint(tracks, ctx) {
        console.warn('Empty Paint strategy!');
    }
}

export default PaintStrategy;